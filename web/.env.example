# Option 1: Veröffentlichtes CSV (keine Auth, öffentlich)
# Einzeln (Publish to web / CSV-Link):
# GOOGLE_SHEETS_CSV_URL="https://docs.google.com/spreadsheets/d/e/2PACX-EXAMPLE/pub?output=csv"
# Mehrere Tabs/CSV-Links (eine URL pro Tabellenblatt):
# - Entweder als kommaseparierte Liste in GOOGLE_SHEETS_CSV_URL
# - Oder in GOOGLE_SHEETS_CSV_URLS (kommasepariert oder Zeile für Zeile)
# Beispiele:
## GOOGLE_SHEETS_CSV_URL="https://.../pub?output=csv, https://.../pub?output=csv"
## GOOGLE_SHEETS_CSV_URLS="
## https://.../pub?output=csv
## https://.../pub?output=csv
## "
# Hinweis: Wir mergen alle CSVs und behalten nur die erste Header-Zeile.

# Nach dem Setzen kannst du prüfen:
#   /api/sheets/health  -> status: ok, mode: csv
#   /api/products       -> Response-Header x-products-source: csv

# Option 2: Service Account (privat)
# Spreadsheet Identität
# GOOGLE_SHEETS_ID="<spreadsheet-id>"
# Service Account Mail
# GOOGLE_SERVICE_ACCOUNT_EMAIL="<service-account>@<project>.iam.gserviceaccount.com"
# Private Key (Variante A: direkt, \n escapen)
# GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nMIIEv...\n-----END PRIVATE KEY-----\n"
# Private Key (Variante B: Base64)
# GOOGLE_PRIVATE_KEY_BASE64="<base64-of-pem>"
# Optional: Tab-Name und Range (passen zu deinem bestehenden Sheet)
# GOOGLE_SHEETS_TAB="Produkte"
# GOOGLE_SHEETS_RANGE="A1:H100000"
# Optional: Mehrere Tabs (kommasepariert) – wir lesen diese Tabs und mergen die Zeilen
# GOOGLE_SHEETS_TABS="Footwear,Tops,Bottoms,Outerwear,Full-Body-Clothing,Headwear,Accessories,Jewelry,Other Stuff"
# Optionale Auto-Erkennung (true/false) für obige Standard-Tabs, wenn GOOGLE_SHEETS_TABS nicht gesetzt ist
# GOOGLE_SHEETS_AUTO_TABS=true

# Zusätzliche Steuerung:
# SHEETS_REQUIRE_AFFILIATE=true   # nur Zeilen mit Affiliate-URL übernehmen (Multi-Agent Schema)
# SHEETS_IGNORE_PLACEHOLDER_LINKS=true  # Zeilen mit example.com/placeholder-Links ignorieren
# PRODUCTS_CACHE_TTL_MS=120000          # In-Memory-Cache für Produktliste (Millisekunden)
